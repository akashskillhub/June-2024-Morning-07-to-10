const demo = (arg: string): boolean => {
    return false
}

demo("john")

// create a fn called findLargest
// two arguments

const findLargest = (n1: number, n2: number): number => n1 > n2 ? n1 : n2 
